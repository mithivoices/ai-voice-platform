// Re-export theme context components for cleaner imports
// This file exists to satisfy react-refresh/only-export-components rule
// which requires files to only export components, not hooks

export { ThemeProvider } from './ThemeContext';
