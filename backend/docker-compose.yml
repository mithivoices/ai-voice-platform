version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ../voice_assets:/voice_assets
      - ../outputs:/outputs
    environment:
      - MODEL_DIR=/voice_assets
